# Jednolite końce linii w repo (LF); Git normalizuje przy commicie.
* text=auto eol=lf

# Pliki binarne
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.xls binary
*.xlsx binary
*.pdf binary
*.woff binary
*.woff2 binary

# Fixtures importu muszą zostać bajt w bajt (np. CSV z mBank: Windows-1250 + CRLF) — bez normalizacji końców linii.
docs/03-dane/fixtures/** -text
**/test/fixtures/** -text
